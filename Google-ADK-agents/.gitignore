# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Env / secrets — never commit real keys
.env
finance_coordinator/.env

# uv
# (uv.lock IS committed — it pins the resolved dependency graph)

# Agent-skill install artifacts (npx skills add) + fetched vendor docs — local
# tooling, reproducible; not app code.
.agents/
.claude/
skills-lock.json
otel.md
