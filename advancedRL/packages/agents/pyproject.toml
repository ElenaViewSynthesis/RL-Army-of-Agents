[project]
name = "trajectoryos-agents"
version = "0.1.0"
description = "TrajectoryOS agent loops: tools, budget enforcement, trajectory recording"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7",
    "trajectoryos-schemas",
    "trajectoryos-core",
    "trajectoryos-environments",
]

[tool.uv.sources]
trajectoryos-schemas = { workspace = true }
trajectoryos-core = { workspace = true }
trajectoryos-environments = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trajectoryos"]
